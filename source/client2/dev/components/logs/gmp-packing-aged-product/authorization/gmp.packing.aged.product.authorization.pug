extends ../log/gmp.packing.aged.product.log.pug

prepend header
  h4 {{log.log_name}}
  .divider

block entryControls
  //-

block buttonRow
  +authorizationButtonRow("Send", "Authorize", "Delete", "Return")